# 后端 API 基础地址（单 bot 场景，默认 bot）。
# 留空时走 vite dev proxy（/api -> VITE_DEV_BACKEND）。
# 生产部署可直接填后端地址，例如 https://betago-admin.example.com
VITE_API_BASE=

# dev proxy 的后端目标（仅 dev 模式生效；单 bot 场景用）。
VITE_DEV_BACKEND=http://localhost:8090

# 多 bot 预设（dev 与 prod 都可用）。
# JSON 数组，字段：id/name/baseURL/token/remark。
#   - 生产环境直接填每个 bot 的 WebUI 公网/内网地址（后端需配好 CORS）。
#   - 开发环境下 baseURL 留空或填真实地址都可以：
#       * 若在本列表，dev server 会把 /bot/<id>/api 代理到 baseURL，
#         前端请求走代理路径，避免跨域。
#
# 示例（生产/多 bot 场景）：
# VITE_BOTS='[
#   {"id":"betago-main","name":"主机器人","baseURL":"https://bot-main.example.com","token":"xxxxxxxx"},
#   {"id":"betago-ops", "name":"运维机器人","baseURL":"https://bot-ops.example.com", "token":"yyyyyyyy"},
#   {"id":"betago-dev", "name":"开发机器人","baseURL":"http://localhost:9091",     "token":"zzzzzzzz"}
# ]'
VITE_BOTS=
